# Browser Agent Production Rescue

Proof #1 uses the pinned Browser Use runtime with a controlled procurement website.

The learner will change only `workspace/`; Browser Use under `sources/browser-use` remains third-party source.
